

// 是否需要 显示/设置  对应的选项
class DateItemModel{
  bool year;
  bool month;
  bool day;
  bool hour;
  bool minute;
  bool second;

  DateItemModel(this.year, this.month, this.day, this.hour, this.minute, this.second);



}